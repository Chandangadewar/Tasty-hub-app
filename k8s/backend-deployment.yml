apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: tastyhub
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: chandan240603/tastyhub-backend:latest
          ports:
            - containerPort: 5000
          env:
            - name: DB_HOST
              value: mysql
            - name: DB_USER
              value: root
            - name: DB_NAME
              value: restaurant_db
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "5000"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tastyhub-secrets
                  key: DB_PASSWORD
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: tastyhub-secrets
                  key: SESSION_SECRET
          readinessProbe:
            httpGet:
              path: /api/health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: tastyhub
spec:
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
